import Login from './Login/Login';
import Register from './Register/Register';
import NotFound from './NotFound/NotFound';

export default { Login, Register, NotFound };
export { Login, Register, NotFound };
